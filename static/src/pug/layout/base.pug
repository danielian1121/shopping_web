doctype html
html
  head
    meta( charset = 'utf8' )
    meta( name = 'viewport', content = 'width=device-width, initial-scale=1.0' )
    title= title

    block stylesheets

  body
    include /component/common/image.pug
    include /component/common/header.pug
    block header
      +header()
    block content

    block footer

    include /component/common/js.pug
    block scripts
